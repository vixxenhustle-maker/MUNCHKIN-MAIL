# Upload These Files To GitHub

This folder is the small Render version of the Munchkin Mailbox.

Upload every file in this folder to a new GitHub repo named:

```text
munchkin-mailbox
```

Then connect that GitHub repo to Render as a Web Service.

Do not upload database passwords here. Put passwords only in Render Environment Variables.
